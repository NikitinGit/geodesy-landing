# Быстрый старт 🚀

## Запуск за 3 шага

### 1️⃣ Откройте проект

```bash
cd /home/igor/IdeaProjects/geodesy-landing
```

### 2️⃣ Запустите локальный сервер

**Вариант A - Используя скрипт:**
```bash
./start-server.sh
```

**Вариант B - Вручную:**
```bash
# Python
python3 -m http.server 8000

# Или PHP
php -S localhost:8000

# Или Node.js
npx http-server -p 8000
```

### 3️⃣ Откройте в браузере

```
http://localhost:8000
```

## Или просто откройте файл

Если не хотите использовать сервер, просто откройте `index.html` в браузере:

```bash
# Linux
xdg-open index.html

# macOS
open index.html

# Windows
start index.html
```

## Структура проекта

```
geodesy-landing/
├── index.html              # ← Главная страница
├── css/
│   ├── style.css          # ← Основные стили
│   └── responsive.css     # ← Мобильная адаптация
├── js/
│   └── main.js           # ← Весь JavaScript
├── images/               # ← Сюда добавляйте изображения
├── send-email.php        # ← Обработчик формы
├── README.md            # ← Полная документация
├── IMAGES-GUIDE.md      # ← Гайд по изображениям
└── start-server.sh      # ← Скрипт запуска сервера
```

## Что можно сделать дальше

### ✅ Базовая настройка (10 минут)

1. **Замените контакты** в `index.html`:
   - Телефон: `+7 (XXX) XXX-XX-XX`
   - Email: `info@geodesy.ru`
   - Адрес: `г. Москва, ул. Примерная, д. 1`

2. **Настройте email** в `send-email.php`:
   ```php
   $to_email = "your-email@example.com"; // ← Ваш email
   ```

3. **Добавьте свой логотип** в папку `images/`

### 🎨 Кастомизация дизайна (30 минут)

Откройте `css/style.css` и измените цвета:

```css
:root {
    --primary-color: #fcb040;  /* ← Основной цвет (кнопки) */
    --dark-bg: #292929;        /* ← Темный фон */
}
```

### 📸 Добавьте изображения (1 час)

См. подробные инструкции в `IMAGES-GUIDE.md`

### 🚀 Деплой на хостинг (15 минут)

**Netlify (бесплатно):**
```bash
# Установите Netlify CLI
npm install -g netlify-cli

# Деплой
netlify deploy --prod
```

**GitHub Pages (бесплатно):**
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/username/geodesy-landing.git
git push -u origin main

# Включите GitHub Pages в настройках репозитория
```

## Функции сайта

✅ **Адаптивный дизайн** - работает на всех устройствах
✅ **Мобильное меню** - бургер-меню для смартфонов
✅ **Плавная прокрутка** - анимированная навигация
✅ **Слайдер отзывов** - автоматическая смена каждые 5 сек
✅ **Форма обратной связи** - с валидацией
✅ **Анимации** - при прокрутке страницы

## Тестирование

### Проверьте на разных устройствах:

1. **Desktop** (1920px+) - полная версия
2. **Tablet** (768px-992px) - адаптированная
3. **Mobile** (320px-768px) - мобильная версия

### Chrome DevTools:
```
F12 → Toggle Device Toolbar → Выберите устройство
```

## Часто задаваемые вопросы

**Q: Форма не отправляет письма?**
A: Настройте `send-email.php` и загрузите на хостинг с PHP. Локально работать не будет.

**Q: Как изменить цвета?**
A: Откройте `css/style.css` → найдите `:root` → измените переменные.

**Q: Как добавить новую секцию?**
A: Скопируйте любую секцию в `index.html` и измените содержимое.

**Q: Нужны ли изображения?**
A: Сайт работает с SVG иконками. Добавьте фото для Hero и логотип.

**Q: Как изменить контент?**
A: Откройте `index.html` и редактируйте тексты напрямую.

## Поддержка

📖 Полная документация: `README.md`
🖼️ Работа с изображениями: `IMAGES-GUIDE.md`
💬 Вопросы: создайте issue в репозитории

---

**Готово! Ваш лендинг готов к использованию! 🎉**

Следующий шаг: добавьте свой контент и изображения, затем опубликуйте на хостинг.
